

# 📘 Документация: Pattern Decoder - Temporal Vision Challenge

## 🎯 Описание задачи

**Название:** Temporal Vision: The Hidden Message  
**Сложность:** Medium  
**Цель:** Обработать и визуализировать временные ряды для обнаружения скрытого слова

### Сюжет
2042 год. Компания Attention Analytics обнаружила зашифрованные сообщения в старых датасетах временных рядов. Группа ушедших data scientists оставила ценные инсайты, спрятанные на виду. Ваша задача — разблокировать секретный паттерн и получить доступ к следующему уровню проприетарных алгоритмов.

---

## 📂 Входные данные

### Структура датасета

**Файлы:** `dataset/dataset_part_1.csv` — `dataset/dataset_part_5.csv`

**Колонки:**
- `timestamp` — временная метка (datetime)
- `value` — числовое значение сигнала
- `series_id` — идентификатор временного ряда
- `noise_level` — уровень шума (float)

### Особенности данных
- Данные разбиты на 5 частей для усложнения анализа
- Содержат как чистый сигнал (`noise_level = 0.0`), так и шумовые точки
- Разные `series_id` представляют отдельные временные ряды
- Скрытый паттерн формируется только чистыми точками

---

## 🔧 Архитектура решения

### Файл: `pattern_decoder.py`

Скрипт выполняет четыре ключевых этапа обработки данных.

---

## 📊 Ключевые компоненты

### 1. Загрузка и объединение данных

```python
all_files = [f'dataset/dataset_part_{i}.csv' for i in range(1,6)]
df = pd.concat([pd.read_csv(f) for f in all_files])
```

**Назначение:**
- Загружает все 5 частей датасета
- Объединяет их в единый DataFrame
- Создает полную картину временных рядов

**Библиотеки:** `pandas`

---

### 2. Предобработка и фильтрация

```python
df['timestamp'] = pd.to_datetime(df['timestamp'])
clean_df = df[df['noise_level'] == 0.0].sort_values('timestamp')
```

**Операции:**
1. **Конвертация типов:** `timestamp` → datetime объект
2. **Фильтрация шума:** Оставляем только `noise_level == 0.0`
3. **Сортировка:** По временной последовательности

**Критически важно:**
- Шумовые точки (`noise_level != 0.0`) — это отвлекающий маневр
- Сортировка необходима для правильного соединения точек
- Чистый сигнал содержит закодированное сообщение

---

### 3. Алгоритм визуализации

```python
prev_point = None
prev_series = None

for _, row in clean_df.iterrows():
    if prev_point and row['series_id'] == prev_series:
        ax.plot([prev_point[0], row['timestamp']], 
                [prev_point[1], row['value']],
                color='black', linewidth=2)
    prev_point = (row['timestamp'], row['value'])
    prev_series = row['series_id']
```

**Логика соединения:**
- **Условие 1:** Существует предыдущая точка (`prev_point`)
- **Условие 2:** Текущая точка принадлежит той же серии (`series_id == prev_series`)
- **Действие:** Рисуем линию между точками

**Почему это работает:**
- Точки из разных серий **не соединяются** → формируются отдельные сегменты
- Каждая серия может представлять букву или элемент слова
- Временная ось (X) и значения (Y) создают визуальную геометрию букв

---

### 4. Генерация визуализации

```python
fig, ax = plt.subplots(figsize=(20, 6))
ax.set_title('Connected Clean Signal Points', fontsize=16)
ax.set_xlabel('Timestamp')
ax.set_ylabel('Value')
plt.xticks(rotation=45)
plt.tight_layout()
plt.savefig('connected_pattern.png', dpi=300)
```

**Параметры:**
- **Размер:** 20×6 дюймов (широкий формат для слова)
- **Разрешение:** 300 DPI (высокое качество)
- **Цвет:** Черные линии (контрастность)
- **Поворот меток:** 45° для читаемости временных меток

**Библиотеки:** `matplotlib.pyplot`

---

## 🎨 Стратегия декодирования

### Принципы работы скрытого паттерна

1. **Пространственное кодирование**
   - Координата X (timestamp) → горизонтальная позиция буквы
   - Координата Y (value) → вертикальные штрихи букв
   - Соединенные точки формируют контуры символов

2. **Сегментация по сериям**
   - Каждая `series_id` может представлять:
     - Отдельную букву
     - Элемент одной буквы (вертикальная/горизонтальная черта)
     - Пробел между буквами (отсутствие точек)

3. **Временная последовательность**
   - Слово читается слева направо по оси времени
   - Сортировка критична для правильного порядка букв

---

## 🔍 Процесс анализа

### Шаг 1: Идентификация чистого сигнала
```
Всего точек: ~N
Шумовых точек: ~M (игнорируются)
Чистых точек: ~K (используются)
```

### Шаг 2: Группировка по сериям
- Подсчитайте уникальные `series_id` в чистом сигнале
- Каждая серия — потенциальная буква/элемент

### Шаг 3: Визуальная интерпретация
- Откройте `connected_pattern.png`
- Ищите буквы, образованные соединенными линиями
- Читайте слева направо

---

## 📤 Выходные данные

### Файл: `connected_pattern.png`

**Содержимое:** График с визуализацией скрытого слова

**Как читать:**
1. Посмотрите на общую картину
2. Идентифицируйте форму букв
3. Соберите буквы в слово
4. Проверьте, что слово имеет смысл (английское слово)

### Формат флага

```
SIGMOID_{HIDDEN_WORD}
```

**Пример:**
- Если на графике видно слово "DATA", флаг: `SIGMOID_DATA`
- Если видно "PATTERN", флаг: `SIGMOID_PATTERN`

**⚠️ Важно:**
- Слово пишется заглавными буквами
- Никаких пробелов внутри фигурных скобок
- Точное соответствие формату

---

## 🛠️ Зависимости

### Требуемые библиотеки

```python
pandas>=1.3.0       # Обработка данных
matplotlib>=3.4.0   # Визуализация
numpy>=1.21.0       # Численные операции (импортирован, но не используется)
scipy>=1.7.0        # Анализ сигналов (импортирован, но не используется)
```

### Установка

```bash
pip install pandas matplotlib numpy scipy
```

---

## 🚀 Запуск

### Структура директорий

```
project/
├── pattern_decoder.py
├── dataset/
│   ├── dataset_part_1.csv
│   ├── dataset_part_2.csv
│   ├── dataset_part_3.csv
│   ├── dataset_part_4.csv
│   └── dataset_part_5.csv
└── connected_pattern.png  (создается после запуска)
```

### Команда запуска

```bash
python pattern_decoder.py
```

### Ожидаемый вывод

```
New visualization saved as connected_pattern.png
```

---

## 🐛 Решение проблем

### Проблема: FileNotFoundError

**Причина:** Файлы датасета не найдены

**Решение:**
```python
# Проверьте существование файлов
import os
for i in range(1, 6):
    path = f'dataset/dataset_part_{i}.csv'
    print(f"{path}: {os.path.exists(path)}")
```

### Проблема: Пустой график

**Причина:** Нет точек с `noise_level == 0.0`

**Диагностика:**
```python
print(f"Total rows: {len(df)}")
print(f"Clean rows: {len(clean_df)}")
print(f"Unique noise levels: {df['noise_level'].unique()}")
```

### Проблема: Слово не читается

**Причины:**
1. Неправильная сортировка
2. Неправильное соединение серий
3. Низкое разрешение изображения

**Решение:**
- Увеличьте DPI: `plt.savefig(..., dpi=600)`
- Проверьте размер: `figsize=(30, 8)`
- Измените цвет фона: `ax.set_facecolor('white')`

---

## 💡 Продвинутые техники

### Альтернативная визуализация

```python
# Цветное кодирование по сериям
for series_id in clean_df['series_id'].unique():
    series_data = clean_df[clean_df['series_id'] == series_id]
    ax.plot(series_data['timestamp'], series_data['value'], 
            marker='o', label=f'Series {series_id}')
ax.legend()
```

### Анализ спектра (если нужно)

```python
from scipy import fftpack

# FFT для обнаружения периодических паттернов
values = clean_df['value'].values
fft = fftpack.fft(values)
power = np.abs(fft)
```

### Статистический анализ

```python
# Группировка по сериям
series_stats = clean_df.groupby('series_id').agg({
    'value': ['mean', 'std', 'min', 'max'],
    'timestamp': 'count'
})
print(series_stats)
```

---

## 📈 Оптимизация

### Ускорение загрузки

```python
# Используйте dtype для оптимизации памяти
dtype_dict = {
    'timestamp': str,
    'value': np.float32,
    'series_id': np.int16,
    'noise_level': np.float32
}
df = pd.concat([pd.read_csv(f, dtype=dtype_dict) for f in all_files])
```

### Векторизация операций

```python
# Вместо циклов используйте pandas операции
clean_df['prev_series'] = clean_df['series_id'].shift(1)
clean_df['is_same_series'] = clean_df['series_id'] == clean_df['prev_series']
```

---

## 🎯 Критерии успеха

✅ **Чек-лист:**
- [ ] Все 5 файлов загружены
- [ ] Фильтрация по `noise_level == 0.0` применена
- [ ] Данные отсортированы по времени
- [ ] Точки соединены только внутри одной серии
- [ ] График сохранен в высоком разрешении
- [ ] Слово визуально различимо
- [ ] Флаг имеет формат `SIGMOID_{WORD}`

---

## 📚 Дополнительные ресурсы

### Концепции
- **Temporal Data Visualization** — визуализация временных рядов
- **Signal Processing** — обработка сигналов и фильтрация шума
- **Steganography** — скрытие информации в данных

### Аналогичные задачи
- ASCII-art генерация из данных
- QR-коды в scatter plots
- Spectrogram art (изображения в звуковых спектрограммах)


---

*Документация создана для задачи "Temporal Vision: The Hidden Message"*  
*Версия: 1.0 | Дата: 2025*
